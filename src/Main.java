public class Main {
    public static void main(String[] args) {
        //  Comparison operators (>, >=, <, <=, ==, !=) return boolean results
        int a = 14;
        int b = 15;
        double d = 3.25;

        boolean result = a > b;

        //  Boolean values can be operated with logical operators
        //  || (or), && (and), ! (not)
        //  Please note no other operators are defined for boolean values

        boolean logicalResult = a > b || a != b;

        //  Please explore comparison and logical operators
    }
}